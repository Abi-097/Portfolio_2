import Snowfall from "react-snowfall";

const Particles = () => {
  return (
    <div>
      <Snowfall snowflakeCount={75} color="#dee4fd" snowflakeSize={15} />
    </div>
  );
};

export default Particles;
